# Environment Configuration Template
# Copy this file to .env.local for development or .env.production for production

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application URL (adjust for your environment)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth.js JWT signing secret (REQUIRED - generate a secure random string)
NEXTAUTH_SECRET="your-secure-random-string-minimum-32-characters"

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Development Database (SQLite)
DATABASE_URL="file:./prisma/dev.db"

# Production Database (MySQL) - uncomment and configure for production
# DATABASE_URL="mysql://username:password@hostname:3306/database_name"

# =============================================================================
# ADMIN CREDENTIALS
# =============================================================================

# Admin login credentials
ADMIN_EMAIL="admin@arvinwedsincia.com"
ADMIN_PASSWORD="Admin123!@#"

# =============================================================================
# MEDIA MANAGEMENT (Cloudinary) - Optional
# =============================================================================

# Cloudinary configuration for image/video uploads
CLOUDINARY_CLOUD_NAME="your-cloudinary-cloud-name"
CLOUDINARY_API_KEY="your-cloudinary-api-key"  
CLOUDINARY_API_SECRET="your-cloudinary-api-secret"

# =============================================================================
# EMAIL SERVICE (Resend) - Optional  
# =============================================================================

# Resend API key for transactional emails
RESEND_API_KEY="re_your-resend-api-key"

# =============================================================================
# MAPS INTEGRATION (Google Maps) - Optional
# =============================================================================

# Google Maps API key for venue locations
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="AIza_your-google-maps-api-key"

# =============================================================================
# PUBLIC CONFIGURATION
# =============================================================================

# Public application URL (used in emails and sharing)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================

# Node environment (automatically set by deployment)
# NODE_ENV="production"

# Production database example:
# DATABASE_URL="mysql://wedding_user:secure_password@mysql.arvinwedsincia.com:3306/wedding_production"

# Production app URL:
# NEXTAUTH_URL="https://arvinwedsincia.com"
# NEXT_PUBLIC_APP_URL="https://arvinwedsincia.com"

# Production admin credentials (use strong passwords):
# ADMIN_EMAIL="admin@arvinwedsincia.com"
# ADMIN_PASSWORD="Very-Secure-Production-Password-2025"

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file:
#    cp .env.example .env.local

# 2. Required variables (minimum for development):
#    - NEXTAUTH_SECRET (generate with: openssl rand -base64 32)
#    - DATABASE_URL (use default SQLite for development)
#    - ADMIN_EMAIL and ADMIN_PASSWORD

# 3. Optional services:
#    - Configure Cloudinary for media uploads
#    - Configure Resend for email functionality  
#    - Configure Google Maps for venue locations

# 4. Verify setup:
#    npm run dev

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - Never commit .env.local or .env.production files to version control
# - Use strong, unique secrets for production environments
# - Rotate secrets regularly in production
# - Use environment-specific databases (SQLite for dev, MySQL for prod)
# - Validate all required variables are set before deployment